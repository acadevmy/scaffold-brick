name: scaffold
description: Generates the scaffold of the devmy monorepo in which the project's applications and libraries will be inserted from the devmy cli.
version: 0.1.1
environment:
  mason: "0.1.0-dev.53"
vars:
  workspaceName:
    type: string
    description: The workspace name
    prompt: What is the workspace name?
